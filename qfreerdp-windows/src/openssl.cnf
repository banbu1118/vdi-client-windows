# OpenSSL configuration for qf-client
# Enables legacy provider (required for MD4/NTLM authentication with Win7)

openssl_conf = openssl_init

[openssl_init]
providers = provider_sect

[provider_sect]
default = default_sect
legacy = legacy_sect

[default_sect]
activate = 1

[legacy_sect]
activate = 1
