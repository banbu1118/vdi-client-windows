# VDI Client 编译安装教程 (Windows)

基于 **Qt 6 + FreeRDP 3.28.0** 的 VDI 远程桌面客户端 Windows 编译指南。

---

## 项目结构

```
VDIClient/
├── freerdp-3.28.0/              # FreeRDP 源码
│   ├── build/                   # CMake 构建目录（编译生成）
│   └── install/                 # 安装目录（头文件 + 库文件）
├── qfreerdp-windows/            # qf-client (QML + FreeRDP)
│   ├── src/
│   │   ├── mini-qf-client.cc    # 主入口，RDP 连接/渲染/音频/剪贴板
│   │   ├── rdp-view-item.h      # QML 渲染节点（D3D11 原生纹理 + QSGSimpleTextureNode）
│   │   ├── usb-manager.cc/h     # USB 设备枚举
│   │   ├── qf_channel_client_handler.c  # 动态虚拟通道处理
│   │   ├── clipboard-entry.h    # 剪贴板文件传输（64KB 分块下载）
│   │   ├── qf_log.h / qf_util.h # spdlog 日志 / 工具函数
│   │   ├── ref-tmp/             # cliprdr 参考实现（C 源码）
│   │   └── main.qml             # QML 界面（全屏/工具栏/悬浮栏）
│   ├── CMakeLists.txt
│   ├── build-qf-client.ps1      # qf-client 编译 + 运行时部署脚本
│   ├── build-freerdp.ps1        # FreeRDP 编译脚本
│   └── build/                   # 编译产物（qf-client.exe + DLL）
├── vdi-client-windows-main/     # VDIClient (Qt Widgets)
│   ├── src/
│   │   ├── main.cpp             # 入口（QApplication + LoginWindow）
│   │   ├── loginwindow.cpp      # 登录/VM 管理/启动 qf-client
│   │   └── loginwindow.h
│   ├── resources/
│   ├── bin/                     # 从 qfreerdp 复制来的完整运行依赖
│   ├── CMakeLists.txt
│   ├── build_and_package.bat    # 清理→复制→编译→部署→Inno Setup 打包 一体化脚本
│   ├── installer.iss            # Inno Setup 打包脚本
│   ├── build/                   # CMake 构建目录
│   └── installer_output/        # 生成的安装包（VDIClient-Setup.exe）
└── 安装教程.md                  # 本文件
```

## 架构设计

### qfreerdp-windows 设计思路

qfreerdp-windows 是一个基于 Qt Quick (QML) + FreeRDP 3 的远程桌面渲染程序，负责实际的 RDP 连接和画面显示。

**核心职责**：
- RDP 协议连接管理（通过 FreeRDP 库）
- 远程桌面画面渲染（QML 场景图中的 D3D11 纹理，通过 QSGSimpleTextureNode）
- 剪贴板重定向（本地 ref-tmp cliprdr 实现 + Qt 剪贴板，默认禁用，`/clipboard` 参数启用）
- USB 设备重定向（libusb 枚举 + FreeRDP urbdrc 通道，`/usb:` 参数启用 + 悬浮栏选择）
- 音频输出（FreeRDP 内建 WASAPI 后端）/ 麦克风输入（WinMM 后端，检测到麦克风设备才启用）
- 摄像头重定向（FreeRDP rdpecam 通道 + WMF 后端，硬编码重定向全部设备 `device:*`）
- 动态分辨率调整（FreeRDP disp 通道）

**渲染架构**：CPU 转存帧缓冲 -> D3D11 原生纹理（`ID3D11DeviceContext::UpdateSubresource` 直接上传）-> `QRhiTexture::createFrom()` 包装 -> `QSGSimpleTextureNode` -> Qt Scene Graph (D3D11) 渲染。纹理只创建一次、每帧复用，无 `QImage` 拷贝，非 DMA-BUF 零拷贝方式，但兼容性好，不依赖特定 GPU 驱动。

```
FreeRDP 线程: H.264 解码 → GDI primary_buffer (堆内存)
    ↓ (EndPaint 回调)
FreeRDP 线程: copyFrameData() → 将脏矩形复制到 CPU staging buffer
    ↓ (QueuedConnection)
Qt 主线程: updateGdiFrame() → 触发 QQuickItem::update()
    ↓ (Scene Graph 同步阶段)
Qt GUI 线程: updatePaintNode() → 设置 RdpFrameTexture 到 QSGSimpleTextureNode
    ↓ (beforeRendering 信号, 渲染线程)
渲染线程: ensureTexture() → CreateTexture2D(BGRA8, DEFAULT, SRV) + createFrom()
渲染线程: uploadFrame() → ID3D11DeviceContext::UpdateSubresource() 上传帧数据
    ↓ (Scene Graph 渲染阶段)
渲染线程 (D3D11): QSGSimpleTextureNode 渲染纹理四边形
```

关键点：
- 从 QRhi 获取 `ID3D11Device`/`ID3D11DeviceContext`（`rhi->nativeHandles()`）
- 原生纹理创建参数必须：`DXGI_FORMAT_B8G8R8A8_UNORM` + `D3D11_USAGE_DEFAULT` + `D3D11_BIND_SHADER_RESOURCE`（缺少 SRV 标志会导致 `createFrom()` 崩溃）
- `commitTextureOperations()` 为空操作（数据已在 beforeRendering 中上传完毕）
- 纹理过滤设为 `Nearest`，提升文字锐利度

**多进程设计**：作为独立进程运行，由 VDIClient 通过 QProcess 启动，通过命令行参数传递连接信息。这种解耦设计使得 RDP 会话崩溃不会影响登录管理界面。

### vdi-client-windows 设计思路

vdi-client-windows 是一个基于 Qt Widgets 的桌面应用程序，负责登录认证、虚拟机管理和 qf-client 生命周期管理。

**核心职责**：
- 用户登录认证（HTTPS API，支持 Schannel/OpenSSL）
- 虚拟机列表获取和展示
- 启动 qf-client 进程并传递 RDP 连接参数
- 心跳保活和会话管理
- 多语言支持（内置翻译字典，无需 .qm 文件）

**进程架构**：
```
VDIClient (Qt Widgets)              qf-client (QML + FreeRDP)
┌──────────────────────┐           ┌─────────────────────────────┐
│ LoginWindow          │  QProcess │ QGuiApplication + QML      │
│ - 登录认证           │────────→  │ - RdpViewItem 渲染         │
│ - VM 列表管理        │  启动     │ - FreeRDP 连接线程         │
│ - 启动 qf-client     │           │ - 剪贴板/USB/音频          │
│ - 心跳保活           │           │ - 动态分辨率               │
└──────────────────────┘           └─────────────────────────────┘
```

**依赖管理**：通过 CMake 的 `add_custom_target(copy_qf_client)` 自动复制 qf-client.exe 及其所有运行依赖（Qt DLL、FreeRDP DLL、FFmpeg DLL、VC++ 运行时等）到统一输出目录，确保所有文件在同一目录下可正确加载。

---

## 1. 系统要求

### 操作系统
- Windows 10/11 64 位

### 工具链
| 工具 | 版本 | 用途 |
|------|------|------|
| Visual Studio 2022 | Community/Professional/Enterprise | C/C++ 编译器 (MSVC) |
| CMake | >= 3.16 | 构建系统 |
| Ninja | 任意版本 | 构建工具（加速编译） |
| Git | 任意版本 | 源码管理 |
| vcpkg | 最新 | C/C++ 依赖管理 |
| Qt 6.11.1 | msvc2022_64 | Qt 框架 |
| Inno Setup 6 | >= 6.0 | 安装包制作 |

### 验证工具安装

```
# 验证 Visual Studio 2022
& "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64

# 验证 CMake
cmake --version

# 验证 Ninja
ninja --version

# 验证 Git
git --version

# 验证 Qt
ls "C:\Qt\6.11.1\msvc2022_64"

# 验证 vcpkg
ls "C:\Users\Administrator\Desktop\workspace\vcpkg\scripts\buildsystems\vcpkg.cmake"

# 验证 Inno Setup
ls "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
```

---

## 2. 安装 vcpkg 依赖

使用 vcpkg 安装 FreeRDP 编译所需的第三方库。

```
vcpkg install zlib:x64-windows openssl:x64-windows ffmpeg[x264]:x64-windows libusb:x64-windows spdlog:x64-windows openh264:x64-windows

```
---

## 3. 安装 Qt 6.11.1

通过 Qt Online Installer 或 Qt Maintenance Tool 安装：

1. 下载地址：https://www.qt.io/download-qt-installer
2. 安装组件选择：
   - **Qt 6.11.1** > **MSVC 2022 64-bit**
   - Developer and Designer Tools > **CMake**、**Ninja**
3. 安装路径：`C:\Qt\6.11.1\msvc2022_64`

关键目录：
- Qt DLL 目录：`C:\Qt\6.11.1\msvc2022_64\bin`
- Qt 插件目录：`C:\Qt\6.11.1\msvc2022_64\plugins`
- Qt CMake 模块：`C:\Qt\6.11.1\msvc2022_64\lib\cmake`

---

## 4. 编译 FreeRDP 3.28.0

### 下载源码

```powershell
cd C:\Users\Administrator\Desktop\VDIClient

# 如果尚未下载
git clone --branch 3.28.0 https://github.com/FreeRDP/FreeRDP.git freerdp-3.28.0
```

### 配置和编译

使用 `build-freerdp.ps1` 脚本一键编译：

```powershell
cd C:\Users\Administrator\Desktop\VDIClient\qfreerdp-windows
.\build-freerdp.ps1
```

脚本执行的操作（等效手动命令）：

```powershell
# 进入 VS 2022 开发者命令提示符
& "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64

# 进入构建目录
cd C:\Users\Administrator\Desktop\VDIClient\freerdp-3.28.0
mkdir build -Force
cd build

# CMake 配置
cmake .. -G Ninja `
  -DCMAKE_BUILD_TYPE=Release `
  -DCMAKE_INSTALL_PREFIX="$PWD\install" `
  "-DCMAKE_TOOLCHAIN_FILE=C:\Users\Administrator\Desktop\workspace\vcpkg\scripts\buildsystems\vcpkg.cmake" `
  -DWITH_WASAPI=ON `
  -DWITH_FFMPEG=ON `
  -DWITH_SWSCALE=ON `
  -DWITH_OPENH264=ON `
  -DWITH_OPENSSL=ON `
  -DWITH_CHANNELS=ON `
  -DWITH_CLIENT_COMMON=ON `
  -DWITH_DSP_FFMPEG=ON `
  -DWITH_SSE2=ON `
  -DCHANNEL_URBDRC=ON `
  -DCHANNEL_RDPECAM_CLIENT=ON `
  -DCHANNEL_GEOMETRY=ON `
  -DWITH_PULSE=OFF `
  -DWITH_ALSA=OFF `
  -DWITH_CUPS=OFF `
  -DWITH_PCSC=OFF `
  -DWITH_VAAPI=OFF `
  -DWITH_GSM=OFF `
  -DWITH_SERVER=OFF `
  -DWITH_PROXY=OFF `
  -DWITH_CLIENT_SDL=OFF `
  -DWITH_MANPAGES=OFF

# 编译（多核并行）
cmake --build . --config Release --parallel

# 安装到 install/ 目录
cmake --install . --config Release
```

> **注**：`build-freerdp.ps1` 脚本会自动检测 VS 2022 并调用 `vcvars64.bat`，无需手动进入开发者命令提示符。未显式指定但默认为 ON 的重要选项：`WITH_CLIENT=ON`（构建 wfreerdp.exe）、`WITH_CCACHE=ON`、`WITH_NATIVE_SSPI=ON`（Windows 原生 SSPI/NLA）、`WITH_WINMM=ON`、`WITH_SIMD/WITH_SSE2/WITH_AVX2=ON`、`WITH_SMARTCARD_PCSC=ON`、`WITH_VERBOSE_WINPR_ASSERT=ON`、`WITH_WINDOWS_CERT_STORE=ON`、`WITH_SERVER_INTERFACE=ON`（服务端接口编译、`WITH_SERVER=OFF` 不构建 shadow server）。默认为 OFF 的还有：`WITH_MEDIA_FOUNDATION=OFF`、`WITH_JPEG=OFF`、`WITH_GFX_AV1=OFF`、`WITH_UNICODE_BUILTIN=OFF`（Windows 平台使用系统 Unicode）。

### 编译产物

```
freerdp-3.28.0/install/
├── include/
│   ├── freerdp3/               # FreeRDP 头文件
│   └── winpr3/                 # WinPR 头文件
├── lib/
│   ├── freerdp3.lib            # FreeRDP 核心库导入库
│   ├── freerdp-client3.lib     # FreeRDP 客户端库导入库
│   ├── winpr3.lib              # WinPR 跨平台层导入库
│   ├── winpr-tools3.lib        # WinPR 工具库导入库
│   ├── cmake/                  # CMake 配置模块（FreeRDP3 / FreeRDP-Client3 / WinPR3 / WinPR-tools3）
│   └── pkgconfig/              # pkg-config 描述文件（freerdp3.pc 等）
├── bin/
│   ├── freerdp3.dll            # FreeRDP 运行时 DLL
│   ├── freerdp-client3.dll     # FreeRDP 客户端库 DLL
│   ├── winpr3.dll              # WinPR 运行时 DLL
│   ├── winpr-tools3.dll        # WinPR 工具运行时 DLL
│   ├── wfreerdp.exe            # FreeRDP Windows 客户端（默认构建）
│   ├── winpr-hash.exe / winpr-makecert.exe   # WinPR 工具
│   └── msvcp140*.dll / vcruntime140*.dll / concrt140.dll   # MSVC 运行库
└── share/freerdp3/            # 数据文件
```

> **注**：`WITH_CLIENT=ON`（默认）会同时构建 Windows 客户端 `wfreerdp.exe`；`WITH_WINPR_TOOLS=ON`（默认）构建 `winpr-hash` / `winpr-makecert` 等工具。本项目的 qf-client 不依赖它们，仅使用库文件。

---

## 5. 编译 qf-client (QML + FreeRDP)

### 配置和编译

```powershell
cd C:\Users\Administrator\Desktop\VDIClient\qfreerdp-windows
.\build-qf-client.ps1
```

等效手动命令：

```powershell
& "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64

cd C:\Users\Administrator\Desktop\VDIClient\qfreerdp-windows\build

cmake .. -G Ninja `
  -DCMAKE_BUILD_TYPE=Release `
  "-DCMAKE_PREFIX_PATH=C:/Qt/6.11.1/msvc2022_64;C:/Users/Administrator/Desktop/VDIClient/freerdp-3.28.0/install" `
  "-DCMAKE_TOOLCHAIN_FILE=C:/Users/Administrator/Desktop/workspace/vcpkg/scripts/buildsystems/vcpkg.cmake"

cmake --build . --config Release --parallel
```

### 编译说明

- CMake 通过 `CMAKE_PREFIX_PATH` 自动查找 FreeRDP install 目录和 Qt 目录
- C++20 标准编译（qf-client 源码），C17 标准编译（cliprdr 等 C 源码）
- 链接库：`Qt6::Core` `Qt6::Gui` `Qt6::GuiPrivate` `Qt6::Qml` `Qt6::Quick`、`freerdp`、`freerdp-client`、`winpr`、`spdlog`、`winmm`、`setupapi`、`libusb`
- `Qt6::GuiPrivate` 提供 QRhi 头文件（`QtGui/rhi/qrhi.h`），用于 D3D11 原生纹理方案
- 剪贴板使用 `src/ref-tmp/` 下的本地 cliprdr 实现（C 源码），通过 `freerdp_channels_client_load_ex` 手动加载；其导出符号与 `freerdp-client3.dll` 内建实现重复，故链接时使用 `/FORCE:MULTIPLE` 允许重复符号
- `resources.rc` + `app.ico` 通过 Windows SDK `rc.exe` 编译嵌入程序图标
- NVIDIA CUDA 相关警告可以安全忽略

### 编译产物

```
qfreerdp-windows/build/
├── qf-client.exe              # 主程序
├── Qt6*.dll                   # Qt 运行时 DLL（Core/Gui/Qml/Quick/QuickControls2 等）
├── freerdp3.dll / freerdp-client3.dll / winpr3.dll   # FreeRDP 运行时
├── avcodec-62.dll / avformat-62.dll / avutil-60.dll  # FFmpeg 媒体库
├── swscale-9.dll / swresample-6.dll  # FFmpeg 缩放/重采样
├── spdlog.dll / fmt.dll       # 日志/格式化库
├── libcrypto-3-x64.dll / libssl-3-x64.dll / legacy.dll  # OpenSSL（legacy 供 MD4/NTLM）
├── openh264-7.dll             # OpenH264 编码库（GFX 使用）
├── libx264-164.dll            # libx264 编码库
├── libusb-1.0.dll             # USB 库
├── z.dll                      # zlib
├── msvcp140*.dll / vcruntime*.dll  # VC++ 运行时
├── openssl.cnf                # OpenSSL 配置（指定 legacy provider）
├── platforms/qwindows.dll     # Windows 平台插件
├── imageformats/  iconengines/    # 图片格式/图标引擎插件
├── qml/                       # QML 运行时模块（QtQml/QtQuick/QtQuick3DUtils）
└── MyTestApp/                 # QML 模块（main.qml）
```

> 部署由 `build-qf-client.ps1` 自动完成（FreeRDP DLL、OpenSSL、FFmpeg、zlib、OpenH264、libx264、Qt DLL、插件、QML 模块、MSVC 运行库）。

### 单独运行测试

```powershell
cd C:\Users\Administrator\Desktop\VDIClient\qfreerdp-windows\build

# 显示帮助信息
.\qf-client.exe --help

# 直接连接远程桌面
.\qf-client.exe /v:192.168.1.90 /u:administrator /p:123456 /cert:ignore /f
```

---

## 6. 编译 VDIClient (Qt Widgets 登录管理)

VDIClient 是一个独立的 Qt Widgets 应用程序。推荐使用一体化脚本 `build_and_package.bat`（清理缓存 → 复制 qf-client → CMake 编译 → windeployqt 部署 → 复制 VC 运行库 → Inno Setup 打包）。

### 配置和编译

```powershell
cd C:\Users\Administrator\Desktop\VDIClient\vdi-client-windows-main
.\build_and_package.bat
```

等效手动命令：

```powershell
& "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64

cd C:\Users\Administrator\Desktop\VDIClient\vdi-client-windows-main\build

cmake .. -G "Visual Studio 17 2022" -A x64 `
  -DCMAKE_BUILD_TYPE=Release `
  "-DCMAKE_PREFIX_PATH=C:/Qt/6.11.1/msvc2022_64"

cmake --build . --config Release
```

> **注**：VDIClient 使用 **Visual Studio 17 2022** 生成器（非 Ninja），Qt Widgets + C++17，仅依赖 Core/Widgets/Network 模块。

### 一键打包流程（build_and_package.bat）

脚本实际包含 7 个逻辑步骤（脚本内打印的编号存在笔误：1~4 显示 `[1/6]`~`[4/6]`，5~7 显示 `[5/7]`~`[7/7]`）：

| 步骤 | 操作 |
|------|------|
| 1 | 清理旧 `build/` 目录 |
| 2 | 从 `..\qfreerdp-windows\build\` 复制 `qf-client.exe`、全部 `*.dll`、`openssl.cnf` 及 `qml/ platforms/ styles/ generic/ iconengines/ imageformats/ networkinformation/` 到 `bin/`；并从 `..\UsbDk_1.0.22_x64.msi` 复制 UsbDk 驱动到 `drivers/` |
| 3 | CMake 配置（`-G "Visual Studio 17 2022" -A x64`，`CMAKE_PREFIX_PATH=Qt`） |
| 4 | 编译 `VDIClient.exe`（Release） |
| 5 | `windeployqt --release --no-translations` 部署 Qt 依赖到 `build\Release\` |
| 6 | 从 VS 2022 Redist 目录复制 VC++ 运行库 DLL 到 `build\Release\`（msvcp140*/vcruntime140*/concrt140/vccorlib140 等） |
| 7 | `ISCC.exe installer.iss` 生成安装包到 `installer_output\VDIClient-Setup.exe` |

> **注**：
> - Inno Setup 自动检测路径顺序：`D:\Inno Setup 6` → `C:\Program Files (x86)\Inno Setup 6` → `C:\Program Files\Inno Setup 6`
> - VC++ 运行库目录固定为 `C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Redist\MSVC\14.44.35112\x64\Microsoft.VC143.CRT`（更换 VS 版本时需同步修改）
> - UsbDk MSI 源文件位于 `VDIClient\UsbDk_1.0.22_x64.msi`（vdi-client-windows-main 的上一级目录），缺失时仅提示警告，不影响打包

### CMake 自动部署机制

`copy_qf_client` 自定义目标（`add_custom_target`）将 `bin/` 目录整体复制到构建目录：

1. 从 `qfreerdp-windows/build/` 复制 `qf-client.exe` 和所有运行依赖（由 build-qf-client.ps1 提前部署）
2. 复制 Qt 运行时 DLL（Core/Gui/Qml/Quick/QuickControls2 等）
3. 复制 FreeRDP 运行时 DLL（`freerdp3.dll`、`freerdp-client3.dll`、`winpr3.dll`）
4. 复制第三方 DLL（OpenSSL、FFmpeg、spdlog、libusb、openh264、libx264、zlib 等）
5. 复制 QML 运行时模块目录
6. 复制 UsbDk 驱动安装包（`drivers/`）

### 编译产物

```
vdi-client-windows-main/bin/
├── VDIClient.exe              # 主程序（登录/VM 管理，位于 build/Release）
├── qf-client.exe              # 远程桌面渲染程序
├── Qt6*.dll                   # Qt 运行时
├── freerdp3.dll 等            # FreeRDP 运行时
├── *.dll                      # 所有第三方依赖
├── platforms/                 # Qt 平台插件
├── imageformats/  iconengines/
├── qml/                       # QML 运行时
└── MyTestApp/                 # qf-client QML 模块
```

---

## 7. 运行 VDI 客户端

### 直接运行

```powershell
# VDIClient.exe 位于 build\Release\（windeployqt 部署的 Qt 依赖同目录）
cd C:\Users\Administrator\Desktop\VDIClient\vdi-client-windows-main\build\Release
.\VDIClient.exe
```

VDIClient 启动后通过 `applicationDirPath()` 查找 `bin/` 目录（当前目录 → 逐级向上），在 `bin/` 下定位 `qf-client.exe`；qf-client 的工作目录设为 `bin/`，所有运行依赖（Qt/FreeRDP/FFmpeg/OpenSSL DLL 及 `qml/` 模块）都与之同目录，因此可直接加载。

### 登录信息

- **服务器地址**：VDI 管理服务器 IP（如 `192.168.1.60`）
- **用户名/密码**：VDI 平台登录凭据
- 登录后显示虚拟机列表，点击 **连接** 后：VDIClient 请求服务器获取连接命令（`command` 字段，原为 wfreerdp.exe 命令）→ 移除程序名、将 `template.rdp` 替换为 `%LOCALAPPDATA%` 下的模板路径 → 以参数方式启动 `bin\qf-client.exe`

### 调试日志

- **VDIClient**：使用 `qDebug()`/`qInfo()` 输出，开发环境在 Qt Creator / Visual Studio 的调试输出窗口查看
- **qf-client**：使用 spdlog 输出到控制台，默认 `info` 级别；可用环境变量 `SPDLOG_LEVEL` 调整（如 `set SPDLOG_LEVEL=debug`），消息格式为 `[action] 内容`

---

## 8. 打包安装包 (Inno Setup)

### 打包脚本

`build_and_package.bat` 最后一步自动调用 Inno Setup：

```powershell
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "C:\Users\Administrator\Desktop\VDIClient\vdi-client-windows-main\installer.iss"
```

打包脚本路径：`vdi-client-windows-main/installer.iss`

主要功能：
- 安装 VDI 客户端到 `Program Files\VDIClient`
- 创建桌面快捷方式和开始菜单项
- 自动安装 VC++ 2022 运行库（如果系统中尚未安装）
- 写入注册表卸载信息

### 安装包产物

```
vdi-client-windows-main/installer_output/
└── VDIClient-Setup.exe        # 可分发安装包
```

---

## 9. 常见问题

### Q: VDIClient.exe 启动无反应（静默崩溃）
- **原因**：缺少运行时 DLL（通常是 Debug 版本的 Qt DLL 与 Release 版本不匹配）
- **解决**：确认 CMake 配置时使用 `-DCMAKE_BUILD_TYPE=Release`，避免链接到 `Qt6Widgetsd.dll` 等 Debug 版本

### Q: 登录报错"网络错误，请检查服务器地址"
- **排查步骤**：
  1. 检查调试输出（`qDebug`）中是否有 TLS 初始化错误
  2. 确认 `tls/` 目录下包含 `qschannelbackend.dll`（Windows 原生 SSL 后端，由 windeployqt 部署；`QSslSocket` 在 Windows 上自动选用 Schannel）
  3. 确认 `loginwindow.cpp` 中 `QSslSocket::VerifyNone` 等 SSL 配置正确
  4. 尝试临时禁用 SSL 证书验证

### Q: 登录提示"不支持的客户端类型"
- **原因**：服务器限制客户端类型
- **解决**：在 `loginwindow.cpp` 中将 `client_type` 设为服务器支持的字符串（当前实现为 `win_client`）

### Q: qf-client.exe 崩溃，错误码 -1073741515
- **原因**：缺少 DLL 依赖（STATUS_DLL_NOT_FOUND）
- **排查**：
  1. 运行 `dumpbin /dependents qf-client.exe` 查看直接依赖
  2. 对每个依赖 DLL 再次运行 `dumpbin /dependents` 检查二级依赖
  3. 确保所有依赖 DLL 都在 `build/` 目录下
- **常见缺失 DLL**：`winpr3.dll`、`spdlog.dll`、`swscale-9.dll`、`swresample-6.dll`、`z.dll` 以及各种 Qt Quick 控件样式 DLL

### Q: qf-client.exe 启动后黑屏/没有画面
- **原因**：QML 运行时模块缺失
- **解决**：确保 `qml/` 目录完整，包含 Qt Quick Controls 2 的 Basic/Fusion/Material/Universal/Windows 等样式模块

### Q: 连接后黑屏闪退（渲染阶段崩溃）
- **原因**：D3D11 原生纹理创建参数不正确，`QRhiTexture::createFrom()` 创建 SRV 时崩溃
- **解决**：检查 `rdp-view-item.h` 中 `RdpFrameTexture::ensureTexture()` 的纹理创建参数：
  - `Format = DXGI_FORMAT_B8G8R8A8_UNORM`
  - `Usage = D3D11_USAGE_DEFAULT`
  - `BindFlags = D3D11_BIND_SHADER_RESOURCE`（**必需**，缺失则崩溃）
  - 纹理需在 `beforeRendering`（渲染线程）中创建，不要在 GUI 线程创建

### Q: 安装包在其他电脑上运行找不到 DLL
- **原因**：Inno Setup 打包时漏掉了某些 DLL
- **解决**：检查 `bin/` 目录是否包含所有必需 DLL，更新 `installer.iss` 中的文件列表

### Q: 编译时提示 `windows.h not found`
- **原因**：CMake 配置时不在 VS 开发者命令提示符下，Windows SDK 路径未设置
- **解决**：使用构建脚本自动调用 `vcvars64.bat`，或在 VS 开发者命令提示符中运行 `cmake`

### Q: 摄像头重定向带宽很高
- **原因**：运行时链接到不含 FFmpeg 支持的 FreeRDP 库
- **解决**：确认 `freerdp.dll` 是自编译版本（含 FFmpeg 支持），而非系统版本
